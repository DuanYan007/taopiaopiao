# 业务高可用故障模型与推进清单

目标：在单机资源受限的前提下，优先完善业务高可用、故障可观测性与并发正确性，不以堆机器和大规模压测为主。

适用范围：`OpenResty -> gateway -> seckill-service -> order-service -> MQ/支付/补偿链路`

## 推进原则

1. 先做故障建模，再补可观测性，再做故障演练与正确性测试。
2. 先处理“会卡主链路、且当前不可见”的问题，再处理“已有限制但缺测试证明”的问题。
3. 每次只推进一个故障点，做到：可注入、可观测、可恢复、可复盘。

## 故障清单

| 编号  | 故障点                     | 影响                   | 当前机制                                             | 主要缺口                     | 下一步                      | 验证方式                   | 状态  |
| --- | ----------------------- | -------------------- | ------------------------------------------------ | ------------------------ | ------------------------ | ---------------------- | --- |
| F1  | Redis 临时不可用             | 锁座失败，消费者处理受阻         | 锁座直接失败；部分 MQ 消费链路依赖重试恢复                          | 缺少明确降级语义与健康暴露            | 定义 Redis 故障时的业务表现与内部状态输出 | 人工停 Redis，观察锁座、消费、恢复表现 | 待推进 |
| F2  | Redis Stream 已写入，但桥接未成功 | 已接受锁座未及时进入正式建单链路     | `pending/unacked` 可被重试或 claim                    | 缺少 backlog 数量、最老堆积时间等可见性 | 增加桥接积压与失败状态暴露            | 人工阻断桥接/发送，观察积压与恢复      | 待推进 |
| F3  | RocketMQ 发送失败           | 建单事件延迟，链路停留在桥接前后     | 桥接发送失败时不确认，可再次消费                                 | 缺少发送失败与积压老化的观测           | 增加发送结果统计与告警阈值            | 人工停 MQ 或注入发送异常         | 待推进 |
| F4  | `LOCK_ACCEPTED` 重复投递    | 重复建单风险               | 已有“查已存在订单再跳过”的保护                                 | 仍需唯一约束与测试证明兜底有效          | 做重复消息幂等测试并核对唯一性约束        | 重放同一消息，多次消费校验订单数       | 待推进 |
| F5  | 正式订单已落库，但 Redis 锁单状态未同步 | 查询结果短暂不一致，恢复依赖补偿     | `RedisLockOrderRecoveryTask` 会回补 `ORDER_CREATED` | 缺少恢复时延、回补量、落后程度的可见性      | 增加恢复任务进度与差距指标            | 人工制造缺口后观察回补收敛          | 待推进 |
| F6  | 支付成功与超时取消并发竞争           | 订单状态可能冲突             | 依赖条件更新，避免互相覆盖                                    | “超时后支付成功”业务语义尚未显式固化      | 明确状态裁决规则并补并发测试           | 并发触发支付/超时，校验最终态        | 待推进 |
| F7  | 取消消息重复投递                | 重复取消、重复解锁、重复日志噪音     | 基于 owner 校验解锁，状态迁移有限制                            | 缺少重复取消的可观测性与行为证明         | 增加重复取消统计并补幂等测试           | 重放取消消息，校验状态与座位释放       | 待推进 |
| F8  | OpenResty 上游超时或后端不可用    | 入口失败，可能产生并发计数泄漏或错误放行 | 已有允许/拒绝/上游相关计数与释放逻辑                              | 超时/5xx 分类不够清晰，缺少泄漏验证     | 强化入口故障分类与 inflight 回收验证  | 人工停后端或制造超时，观察 gate 状态  | 待推进 |

## 推荐推进顺序

### 第一阶段：入口与基础依赖可用性

- F8 OpenResty 上游超时或后端不可用
- F1 Redis 临时不可用

### 第二阶段：异步桥接与恢复能力

- F2 Redis Stream 已写入，但桥接未成功
- F3 RocketMQ 发送失败
- F5 正式订单已落库，但 Redis 锁单状态未同步

### 第三阶段：并发竞争与幂等证明

- F4 `LOCK_ACCEPTED` 重复投递
- F6 支付成功与超时取消并发竞争
- F7 取消消息重复投递

## 单项推进模板

每推进一个故障点，只回答以下 5 个问题：

1. 如何注入故障？
2. 期望系统表现是什么？
3. 需要补哪些可观测性？
4. 如何验证恢复完成？
5. 这项能力最后如何写进简历？

## F8 细化设计

### 当前判断

- `/api/seckill/lock` 在 OpenResty 中先执行 `access_by_lua`，放行后再反向代理到 gateway。
- inflight 释放走了双保险：`header_filter_by_lua` 和 `log_by_lua` 都会调用释放逻辑，并且靠 `ngx.ctx.seckill_gate_inflight_acquired` 防止重复释放。
- 因此，F8 当前更大的问题不是“完全没有释放”，而是“故障发生后看不清到底发生了什么”。

### 现有观测的关键问题

- `/internal/seckill/gate/status` 目前只暴露了放行/拒绝计数，以及少量上游结果计数。
- 其中 `upstream_unavailable` 的语义其实是“座位不可用”这一业务结果，不是“上游服务不可用”。
- `capture_upstream_result()` 依赖响应体 JSON 解析；如果 gateway 连接失败、返回 502/504、超时、空响应、非 JSON，当前基本无法准确分类。
- 这意味着 F8 发生时，现有状态接口容易把“业务失败”和“基础设施失败”混在一起。

### F8 第一轮目标

只做低成本、强收益的增强，不做复杂重构：

1. 保持现有 inflight 释放模型不变，先不改并发控制主逻辑。
2. 把“业务结果分类”和“基础设施故障分类”明确拆开。
3. 让 `/internal/seckill/gate/status` 能直接看出：是 gateway 挂了、超时了、返回 5xx，还是只是业务拒绝。

### 建议新增的观测维度

- `infra_upstream_connect_fail`
- `infra_upstream_timeout`
- `infra_upstream_5xx`
- `infra_upstream_non_json`
- `infra_upstream_empty_body`

同时保留现有业务计数，但建议逐步把名字改清楚：

- `upstream_unavailable` -> 更准确地表达为“seat unavailable”语义
- `upstream_other` -> 只保留给“未知业务返回”，不要混入基础设施故障

### 最小推进方案

1. 在 `body_filter` 继续处理业务 JSON 分类。
2. 在 `log_by_lua` 或等价阶段补充基于 HTTP 状态和上游状态的基础设施分类。
3. 在 `status` 接口中同时输出业务计数与基础设施计数。
4. 明确 F8 的验收标准：入口失败后 inflight 必须回到 0，且故障类型可被区分。

### 建议先做的两种演练

1. 停掉 gateway，验证连接失败场景。
   期望：请求快速失败，inflight 回收，基础设施失败计数增加。
2. 制造上游慢响应，验证超时场景。
   期望：请求按预期超时，inflight 回收，不误记为业务失败。

### 这一项做完后的价值

- 能证明入口层不仅有限流，而且具备故障识别与快速恢复能力。
- 能把“业务不可用”和“系统不可用”分开观测，后续才能继续推进 Redis、MQ、补偿链路的高可用治理。
