#!/usr/bin/env bash

set -euo pipefail

BIN_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

SESSION_ID=3 \
USERS="${USERS:-16000}" \
USER_BASE="${USER_BASE:-400000}" \
SEAT_START_ID="${SEAT_START_ID:-1761}" \
SEAT_END_ID="${SEAT_END_ID:-17760}" \
SEATS_PER_REQUEST="${SEATS_PER_REQUEST:-1}" \
MAX_ATTEMPTS_PER_USER="${MAX_ATTEMPTS_PER_USER:-20}" \
REQUEST_INTERVAL_SECONDS="${REQUEST_INTERVAL_SECONDS:-1}" \
MAX_DURATION="${MAX_DURATION:-120s}" \
STATUS0_LOG_LIMIT_PER_VU="${STATUS0_LOG_LIMIT_PER_VU:-1}" \
bash "${BIN_DIR}/run-lock-only-burst.sh"
