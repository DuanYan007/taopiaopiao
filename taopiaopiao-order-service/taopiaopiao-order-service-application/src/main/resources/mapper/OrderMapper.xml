<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.duanyan.taopiaopiao.orderservice.application.mapper.OrderMapper">

    <update id="markPaidIfUnpaid">
        UPDATE orders
        SET status = #{paidStatus},
            pay_time = NOW(),
            updated_at = NOW()
        WHERE order_no = #{orderNo}
          AND status = 1
    </update>

    <update id="markTimeoutIfUnpaid">
        UPDATE orders
        SET status = #{timeoutStatus},
            cancel_time = NOW(),
            updated_at = NOW()
        WHERE order_no = #{orderNo}
          AND status = 1
    </update>

</mapper>
