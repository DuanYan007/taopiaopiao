server:
  port: 7500

spring:
  application:
    name: payment-system

# Knife4j API文档配置
knife4j:
  enable: true
  setting:
    language: zh_cn
  production: false

# 日志配置
logging:
  level:
    root: INFO
    com.duanyan.payment: DEBUG
